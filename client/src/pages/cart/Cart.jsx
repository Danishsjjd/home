import React from "react";
import MountTransition from "../../utils/MountTransition";

const Cart = () => {
	return (
		<MountTransition>
			<div className="mt-16 max-w-7xl mx-auto">Cart</div>
		</MountTransition>
	);
};

export default Cart;
